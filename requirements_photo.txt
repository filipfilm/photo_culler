# Core dependencies
rawpy>=0.18.0
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0
click>=8.1.0
requests>=2.25.0

# Vision model dependencies (choose one):

# Option 1: CLIP (PyTorch-based, GPU recommended)
# torch>=2.0.0
# torchvision>=0.15.0
# clip-by-openai

# Option 2: Ollama (Local LLM, works on CPU/GPU)
# No additional Python packages needed - just install Ollama:
# https://ollama.ai