click>=8.1.0
numpy>=1.24.0
Pillow>=10.0.0
requests>=2.28.0
tqdm>=4.64.0
PyYAML>=6.0

# Blur analysis. Without OpenCV the culler still runs, but the technical
# sharpness cross-check is disabled and every borderline photo goes to Review.
opencv-python>=4.8.0

# Burst grouping and duplicate detection.
ImageHash>=4.3.0
