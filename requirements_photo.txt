# Core dependencies
rawpy>=0.18.0
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0
click>=8.1.0
requests>=2.25.0
PyYAML>=6.0

# For similar image detection (optional but recommended)
imagehash>=4.3.0

# Vision model - Ollama recommended for Mac
# Install Ollama from https://ollama.ai
# Then run: ollama pull llava:13b

# Alternative: CLIP (PyTorch-based, GPU recommended)
# torch>=2.0.0
# torchvision>=0.15.0
# clip-by-openai